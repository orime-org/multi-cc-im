# Claude Code local hook runtime (logs, caches, transient state)
.remember/

# Playwright MCP browser session artifacts (snapshots, console logs)
.playwright-mcp/

# Node / pnpm
node_modules/
.pnpm-store/

# TypeScript build outputs
dist/
*.tsbuildinfo

# Test coverage
coverage/

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp

# Project-local Claude Code workspace (hooks, settings, scratch files).
# Personal per-developer — not shared via git. Includes:
#   - hooks/* (UserPromptSubmit workflow reminder + Stop neat-freak nudge)
#   - settings.local.json (per-machine Bash permission allowlist)
#   - smoke-test-*.txt (runtime scratch for sensitive-path gate triggers)
.claude/
