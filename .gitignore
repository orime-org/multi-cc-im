# Claude Code local hook runtime (logs, caches, transient state)
.remember/

# Playwright MCP browser session artifacts (snapshots, console logs)
.playwright-mcp/

# Node / pnpm
node_modules/
.pnpm-store/

# TypeScript build outputs
dist/
*.tsbuildinfo

# Test coverage
coverage/

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp

# v1.12 PermissionDialog smoke-test scratch files (created at runtime to
# trigger cc's sensitive-path gate; throwaway, never meant to be tracked)
.claude/smoke-test-*.txt
