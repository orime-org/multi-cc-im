# 项目技术规范与状态

> 本文是 [`CLAUDE.md`](../CLAUDE.md) 的技术补充。
>
> - **CLAUDE.md** = AI 干活纪律（找根因 / DD 流程 / 编码行为准则 / 通用工程规范）
> - **本文（conventions.md）** = 项目特定的技术实现规范、状态总表、修订记录、参考资料
>
> CLAUDE.md 没有的项目特定规则（hook timeout / send-text 两步法 / 路由 key 选择 / 禁止 `#$<paneId>` / etc.）都在本文。CLAUDE.md 引用本文为「依赖文档」。

---

## 项目简介（详细版）

`multi-cc-im` —— 个人本地 bridge：通过飞书 (Lark) IM 把跑在 **WezTerm tab 里的多个 Claude Code session** 暴露到手机，实现"在公司用控制台 + 外面用 IM"双客户端 + `#session` 路由 + cc 用量分析 + 多 IM/term/CLI 可扩展。

> **状态**：v1.12 P1-P7 主线落地（2026-05-13）—— [v1.12 PermissionRequest hook IM bridge DD](superpowers/specs/2026-05-13-permission-request-hook-bridge-dd.md) 9 维度全 user-locked + 实施完成。订阅 cc 内部 `PermissionRequest` hook event 把 `.claude/* / .git/* / .env*` 等 cc hard-coded sensitive path TUI dialog forward 到 IM。`/start auto` 自动 allow 一次 + IM 发审计提示 (D2-A + D5-B 单次允许不偷写 session rule)；`/start off` 把 cc's `permission_suggestions` 渲染成 numbered options forward IM (D3-A + D4)，user 在 IM 选 1 (single-yes) / N+1 (始终允许...) / N+2 (拒绝) — 选始终允许时 daemon 解析 cc 自己提供的 PermissionUpdate 写回 `decision.updatedPermissions` 注入 `alwaysAllowRules.session` (D6-A 只能选 cc 给的，不许 AI 自创)。**真账号 smoke + P8/P9 docs 本 PR 落地**。v1.9 D5-D 重构落地（2026-05-12）—— [v1.9 AskUserQuestion IM bridge DD §9](superpowers/specs/2026-05-12-askuserquestion-im-bridge-dd.md#9-revision-d5-retracted--allow--updatedinputanswers-is-the-correct-channel) R1-R10 单 PR 完成：D5 通道由 `deny + reason` 改为 `allow + updatedInput.answers`（官方 [agent-sdk/user-input docs](https://code.claude.com/docs/en/agent-sdk/user-input#handle-clarifying-questions) documented 路径）。hook 写 `{permissionDecision: 'allow', updatedInput: {questions, answers}}`，daemon 查 `options[index-1].label` 组装 answers map（option/multi-select/text 三态），timeout 110s 后 hook 自构造空字符 answers；timeouts 同步缩到 110s hook / 120s matcher / 130s reaper；late-reply hook 已退情况 IM 提示「⏱ cc 已超时，本轮不再等待」。原 v1.10 force-deny override + v1.11 reaper 30s bump 一并清理。v1.9 P1-P4 + P6 原方案已主线落地（PR #131-#135 + #139/#140 v1.10/v1.11 polish）。原方案 user-locked D1-D4 + D6 不变（IMWork on 总 forward、hook hold、numbered IM 格式、AI 自然语言分诊、free text 支持）。v1.8 全实施完成（2026-05-12）—— 路由前缀 `@` → `#` 硬切换（[DD 2026-05-12](superpowers/specs/2026-05-12-routing-symbol-change-dd.md)）：飞书 (以及多数现代 IM) 把 `@` 字符在客户端层 rewrite 为 mention object，bridge parser 拿不到字面 `@<tab>` 字符串；改用 `#` 后零 IM 层 rewrite、跟 Slack/Discord/IRC channel 约定一致、各端键盘易输入。Hard cutover 无 compat / dual-syntax / escape。v1.7 全实施完成（2026-05-11）—— v1.6 wizard + DD #86 §11.4 全 milestones 之上接续完成 [v1.7 natural-language IM permission replies](superpowers/specs/2026-05-11-im-permission-natural-language-dd.md) P1-P6。**M5 interactive cards ❌ 已取消**（2026-05-11，DD #86 §11.5）。当前 packages：shared / storage-files / im-lark / term-wezterm / cli-cc / bridge。架构 → [`architecture.md`](architecture.md)；DD → [`superpowers/specs/`](superpowers/specs/)；用户上手 → [`../README.md`](../README.md) Quick Start；开发命令 → [`dev.md`](dev.md)。Follow-up：Lark intl WSClient 真账号 smoke / 后续 tg IM adapter / analytics package / **v1.9 AskUserQuestion IM bridge P5 真账号 smoke pending**（P1-P4 + P6 已合并主线，C2 keystone 风险待 user 真账号验证：cc 是否真把 deny+reason 当 user answer 解读 / AI prompt 对实际 widget 内容是否能正确匹配 option label / 自然语言回复在 v1.7 ai-router infrastructure 上下文是否稳定）。**Lark group-chat 支持 ❌ 永久不做**（[DD 2026-05-12](superpowers/specs/2026-05-12-lark-group-chat-support-dd.md) D1-1 锁定：privacy creep / state 复杂度 / scope drift / owner-only ACL 脆弱 / 个人 use case 没真实需求 — 详见 DD §6.x）。**Lark audio msg 处理 ❌ 永久不做**（[DD 2026-05-12](superpowers/specs/2026-05-12-lark-audio-msg-handling-dd.md) D1-1 锁定：飞书手机键盘 🎤 已是 system-level STT，99% 真实使用走 text 即可；adapter 收到 audio msg 改回友好提示「请用键盘麦克风转文字」替代之前 silent drop）。

### 修订记录

- 2026-04-26 v0.1（初稿）→ v0.2（撤回 share 假设）
- 2026-04-27 v0.3（cc hook + wezterm cli 实测完成，6 项假设升 ✓）
- 2026-05-05 v1.0（实施完成 PR #4-#46，6 packages 到位）
- 2026-05-07 v1.1（permission forward PR #51-#53 + voice/image 通路实施）
- 2026-05-08 v1.2（IMWork + IMOrigin + read-only 白名单 + reaper PR #55+）
- 2026-05-09 v1.3（daemon liveness 检测：daemon.pid lock + 双开检测 + Ctrl+C 清理 + hook 4 层 short-circuit decision tree）
- 2026-05-09 v1.4（AI 路由 plain IM 消息：daemon 每条 plain msg 起一个 `claude --print` 子进程做 triage + intent 提取；bare `/<cmd>` 替换 `@multi-cc-im /<cmd>` daemon 命令语法，无 backwards compat）
- 2026-05-09 v1.5（IM adapter 切换：wechat 整包删除（含 vendor OpenClaw）+ openclaw shim 删除 + IMReplyContext `'wechat'` variant 删除 + lark adapter 接 `@larksuiteoapi/node-sdk` WSClient 长连接（DD #86）。M1 wechat purge / M2 lark login / M3 lark adapter 完成；M5 + M7-M8 进行中）
- 2026-05-10 v1.5.1（PR #93 M7 daemon orchestration 完成 + PR #94 hotfix：AI router 子进程 `WEZTERM_PANE` env leak → 子进程 stop hook 不再误 forward 路由 JSON 给 IM；plain AI 路由成功 echo 改两行 X 格式 (`target: <tab>\ncontent: <intent摘录>`) + intent 截前 20 字；分诊失败 echo 加 raw IM 摘录）
- 2026-05-10 v1.6（[interactive start/setup wizard DD](superpowers/specs/2026-05-10-interactive-start-wizard-dd.md) 锁定：单 `start [<adapter>]` 命令；@clack/prompts v1.3.0 prompt 库；inline ASCII + ANSI hyperlink 配置指南；字段-typed AWS-style mask（secret 字段 `'*'*16 + last_4`，非 secret 全显）；hybrid schema-driven adapter setup interface + adapter `validate(values)` callback；W1-W8 实施 milestones）
- 2026-05-11 v1.6 全实施完成（PR #96 W1 deps / #97 W2 shared schema interface / #98 W3 lark setupSchema + validateLarkCredentials 抽出 / #99 W4 generic wizard + AWS-style mask / #100 W5 selector + start.ts rewire / #101 W6 inline guide + OSC 8 hyperlinks + docs/setup-feishu.md / #102 W7 login shortcut unify through schema persist; DD #86 §11.4 M8 docs polish 同 PR 收尾）
- 2026-05-11 真账号 smoke 一系列修复（PR #104 cold-start race + SDK info 噪音抑制 / #105 AI router prompt lenient + 失败 echo 加 tab 列表 / #106 markdown strip cc → IM / #107 start banner 清理 + IM `/start` next-step 提示 / #108 IM 入站 message_id 去重 + #109 静默去重 / #110 AI router topic-mention prompt + substring fallback + daemon stderr 多行 echo 展开 / #111 prompt 全改英文 + `[AI router]` reason trace log / #112 Stop / PreToolUse 文件 delete-always 语义 / #113 AI router exec 错误诊断升级 + timeout 15s→30s）
- 2026-05-11 M5 interactive cards ❌ 取消（DD #86 §11.5 final decision：飞书 cards 回调只走 webhook，与 "no public IP" 核心约束冲突；保留 `/1`/`/2` 文字流 forever；M5 不重新排期，除非飞书改协议或项目放弃 no-public-IP 约束）
- 2026-05-11 v1.7 DD 锁定（[natural-language IM permission replies](superpowers/specs/2026-05-11-im-permission-natural-language-dd.md)）：用户在 PreToolUse 转发的 IM 消息里用自然语言（"multi-cc-im 那个我同意" / "node 的拒绝"）回审批，daemon 跑 AI 分诊 tab + decision；D1 保留 `#<tab> /1 /2` 作 deterministic 兜底；D2 每条消息扫盘 `*.PermissionRequest.*` 喂 AI prompt；D3 `RouterResult.aiTrace` 扩 `permissionResponse?`；D4 daemon 前置 gate（有 pending Request 才进 permission 模式）；D5 非对称信任 + `[AI permission]` trace log
- 2026-05-11 v1.7 全实施完成（PR #122 P1 `listPendingPermissionRequests` helper in cli-cc / PR #123 P2 ai-router prompt 加 PENDING 块 + D5-3 match-signal 规则 + 输出 schema 加 `permissionResponse` 字段 / PR #124 P3 router `handlePlainWithAI` 注入 pending → AI → 短路到 `RouterResult.permissionResponse` 分支 / PR #125 P4 orchestrator dispatch via `handlePermissionResponseFromIM(reason?)` + `[AI permission]` 独立 audit log 行 + cli-cc paneId → router 品牌 `PaneId` IPC 桥接 / 本 PR P6 docs。P5 实施测试折进各 PR 单元覆盖。Bundle 0.0.1 验证 OK，736/736 tests pass）
- 2026-05-12 v1.8 全实施完成（[routing symbol change DD](superpowers/specs/2026-05-12-routing-symbol-change-dd.md)）：路由前缀 `@` → `#` 硬切换。飞书真账号 smoke 暴露 `@` 被客户端 mention picker rewrite 为 mention object，parser 拿不到字面字符；改 `#` 后零 IM 层 rewrite + 跟 Slack/Discord/IRC channel 约定一致。21 文件 / 366 处机械替换：parser tokenizer + matcher / router / orchestrator / ai-router 注释与 echo + 全部 tests + README ×2 + dev.md + architecture.md + conventions.md。Hard cutover 无 compat / dual-syntax / escape。同 PR 修 `/start` echo 漏掉的 v1.4 AI 分诊 + v1.7 自然语言审批两条 bullet（user issue 2026-05-12 (a)）
- 2026-05-12 v1.8 hotfix + group-DD 锁定：sweep PR #127 漏掉的 5 处错描述「当前 toggle 走 `@multi-cc-im /start`」的滞后注释（hook-receiver / state-files / orchestrator / shared cli / term-wezterm tab-title），改成 bare `/start` / `#<tabname>` / `#<query>` 等当前实际语法；matcher.test 描述 `@multi-cc-im` → `#multi-cc-im`。同 PR 锁定 [lark group-chat support DD](superpowers/specs/2026-05-12-lark-group-chat-support-dd.md) 为 **D1-1 ❌ 不做**，理由 5 条（privacy creep / 多 context state 复杂度 / scope drift / owner-only ACL 脆弱 / 个人 use case 无真实需求），P1-P6 cancel 但保留历史档以备未来需求触发 reopen。同时复核 AI 分诊 model `claude-haiku-4-5`：[Anthropic 官方 docs](https://platform.claude.com/docs/en/docs/about-claude/models/overview) 明确标 Haiku 4.5 = "Fastest" tier，currently-available 列表无更快可选，保持不变。
- 2026-05-12 audio-msg DD 锁定 + adapter echo 补丁：[lark audio msg handling DD](superpowers/specs/2026-05-12-lark-audio-msg-handling-dd.md) 全 6 维度尽调（Lark audio payload `{file_key, duration_ms}` + 100M resource API + Lark 自带 STT `speech_to_text.speech.fileRecognize` 60s 上限 + Anthropic Files API 不收 audio + Whisper API $0.006/min + whisper.cpp tiny→large 模型 75 MB→2.9 GB + 用户加 D6 pre-ack 需求），用户最终拍板「服务器支持直接处理语音，但只支持处理文本」走 simple-path：D1-1 不做 audio msg pipeline，依赖飞书手机键盘 🎤 system-level STT 已生效（99% 真实使用走文字）；adapter.ts `msg_type === 'audio'` 分支从 silent drop 改为友好 echo「❌ 暂不支持音频消息，请用键盘 🎤 麦克风转文字后发送」；2 adapter test 覆盖（echo 调用 + echo failure 不阻断）。
- 2026-05-12 v1.9 DD 锁定（[AskUserQuestion IM bridge DD](superpowers/specs/2026-05-12-askuserquestion-im-bridge-dd.md)）：cc 内置 `AskUserQuestion` widget 当前 fire `PreToolUse` 后被 daemon auto-mode silent-allow 吞掉 / ask-mode 走 `/1` `/2` 语义不对（这是 widget 选项不是允许拒绝）。用户全 6 维度 locked：D1-B（IMWork on 总 forward 不管 auto/off）/ D2-B（hook hold 等 IM 回复，TUI 不渲染 widget）/ D3 numbered options + 你的考虑 / D4-B（AI 自然语言分诊）/ D5-C（hook 返 cc `permissionDecision: 'deny' + permissionDecisionReason: <answer>`，cc 自己解读 reason 当用户答复，**无 keystroke 注入**）/ D6-B（free text 支持）。P1-P6 实施 pending：settings.json per-tool timeout 拆分（AskUserQuestion 5min / 其余 10s）/ hook-receiver special-case branch / orchestrator 格式化 / ai-router prompt 扩展 option 匹配 / tests + smoke C2 风险（cc 是否真把 deny+reason 当 answer 用） / docs。
- 2026-05-12 v1.9 P1-P4 + P6 主线落地（PR #131 P1 `setup-hooks.ts` PreToolUse 拆 2 disjoint matchers — `AskUserQuestion` literal/timeout 300s + 负向 lookahead `^(?!AskUserQuestion$).+$`/timeout 20s — 因为 cc 是「所有 matching hooks 并发触发 + identical handler dedup」，不同 timeout 不算 identical 会双触发 / PR #132 P2 hook-receiver decision tree 加 `isAskUserQuestion` 特判分支：跳过 E1.5 auto-allow short-circuit（D1-B）+ `ASK_USER_QUESTION_TIMEOUT_MS = 290_000` 内部轮询超时 + 响应到达时强制 `decision='deny'` 防御 rogue daemon（D5-C invariant）+ timeout 兜底默认 allow 让 cc TUI 渲染 widget fallback（DD §7）/ PR #133 P3 `orchestrator.handlePreToolUse` 加 AskUserQuestion 分支：调用新 `formatAskUserQuestionPrompt()` 渲染数字编号 + label + description + 「你的考虑」自由文本项 + 多问题 fallback note + 防御 malformed payload；audit log `[AskUserQuestion forward pane=X tab=Y] questions=N options=M` / PR #134 P4 ai-router `renderPendingBlock` 按 `toolName` 分发 bullet 格式：AskUserQuestion 多行（question + options + descriptions）+ SPECIAL RULE 节仅在 pending 含 AskUserQuestion 时附加（always deny / reason=exact label 或 verbatim free text / D5-3 不适用） / 本 PR P6 README ×2 + conventions docs。**P5 真账号 C2 smoke pending** —— cc 是否真把 deny+reason 当 answer 用 + AI prompt 实战是否能准匹配 option label / free text）。Bundle 0.0.1 验证 OK，753/753 tests pass。
- 2026-05-12 v1.10 force-permission mode + AI router pre-ack（PR #139）：daemon 检测 pending 非空时 force AI 走 permission 通道，禁止顶层 target/intent；新 `renderForcePermissionPrompt` strip routing rules；IM 入站发 plain msg 前先 echo `🔍 AI 分诊中: "<excerpt>"` 掩盖 3-7s 子进程冷启动等待。修复 v1.9 P5 smoke「@multi-cc-im 那个」被 AI 走 routing 模式错误转发的根因（AI Rule 1「tab name appears → PICK」覆盖 permission 通道）。
- 2026-05-12 v1.11 per-tool reaper delay（PR #140）：v1.10 后 P5 smoke 仍然没过，root cause 是 reaper `DEFAULT_REAPER_DELAY_MS = 10_000` 在 +10s 把活的 PermissionRequest 文件 unlink 了（用户读 IM prompt + 打字 >10s）。`ASK_USER_QUESTION_REAPER_DELAY_MS = 310_000` 加上、`DEFAULT_REAPER_DELAY_MS` 由 10s 升 30s（同时修 v1.7 NL Bash deny 同样 10s 偏紧问题），加 `[reaper] unlink ... tool=T delay=Nms` 诊断 log。
- 2026-05-12 v1.9 D5 retracted ([DD §9](superpowers/specs/2026-05-12-askuserquestion-im-bridge-dd.md#9-revision-d5-retracted--allow--updatedinputanswers-is-the-correct-channel))：v1.10/v1.11 smoke 看似跑通后用户推翻 D5-C — cc transcript 显示 AUQ tool denied 语义错位。重查官方 [agent-sdk/user-input docs](https://code.claude.com/docs/en/agent-sdk/user-input#handle-clarifying-questions) 暴露遗漏候选 **D5-D `allow + updatedInput.answers`** —— PreToolUse hook 输出顶层 `updatedInput` 字段，对 AUQ 工具明确 documented 用 `{questions, answers}` shape 把答案注入 tool input，cc 把 tool 当 succeeded 跑完，transcript 记录正常 tool result 而非 deny。原 DD §3 D5 行 / §5 表 / §8 review log 加注脚 + 新 §9 修订章节 + R1-R10 实施清单。Timeouts 同步缩到 110s hook poll / 120s matcher / 130s reaper（v1.11 的 300s 是基于错误 channel 假设，120s 真实交互够用 — user 拍板）。Timeout 路径 hook 自构造 `answers[q.question] = ''` 空字符（不走 deny 通道，协议一致性）。迟到 reply 加 IM 提示「⏱ cc 已超时，本轮不再等待」。根因：DD 起草时只翻 hooks docs 没翻 agent-sdk/user-input docs，违反 [`feedback_dd_question_premise.md`] 候选穷举 + [`feedback_upstream_schema_real_smoke.md`] upstream schema 必须 fetch 所有官方 docs 页面 — 两条 lesson 已沉淀。
- 2026-05-12 v1.9 D5-D R1-R10 实施落地（feat(auq) 单 PR）：R1 `packages/shared/src/ask-user-question.ts` 新建 + 三个 zod schema (`AskUserQuestionToolInputSchema` / `AskUserQuestionAnswerSchema` discriminated union option/text / `AskUserQuestionAIOutputSchema` AI envelope) + 12 单测。R2 `packages/cli-cc/src/state-files.ts` `PermissionResponseFile` 改 discriminated union (`allow` + optional `updatedInput?` + optional `reason?` / `deny` + required `reason`)，writer 走 zod parse runtime 校验。R3 `packages/cli-cc/src/hook-receiver.ts` 改读 union → 输出 `{permissionDecision: 'allow', updatedInput?}` / `{deny, reason}`；移除 v1.10 `isAskUserQuestion → force deny` 硬死逻辑；hook timeout 自构造 `answers[q.question]=''` 空字符走 allow + updatedInput（不走 deny 通道）。R4 `apps/multi-cc-im/src/setup-hooks.ts` AUQ matcher `timeout: 300 → 120`。R5 `packages/bridge/src/orchestrator.ts` `ASK_USER_QUESTION_REAPER_DELAY_MS: 310_000 → 130_000`。R6 `packages/bridge/src/ai-router.ts` 新 `renderAskUserQuestionPrompt` + `parseAskUserQuestionOutput` + `routeAskUserQuestionViaAI`，AUQ AI 输出 schema 走 `AskUserQuestionAIOutput`（target + reason + answers 数组）；通用 force-perm prompt 删 `formatAskUserQuestionPendingBullet` + `ASK_USER_QUESTION_RULES` dead code。R7 `packages/bridge/src/router.ts` `RouterOpts.aiAskUserQuestionRouter` 注入 + `RouterPermissionResponse.updatedInput?` 字段；handlePlainWithAI 检测 AUQ pending 分流到 `handleAskUserQuestionReply`，daemon 查 `questions[i].options[j-1].label` 组装 answers map（option / multi-select array / text 三态 + 缺失 q 填空字符）。R8 IM echo 二态 (`你答 ① <label>` / `自由回答: <text>`) + late-reply dead-drop 提示「⏱ cc 已超时，本轮不再等待你的回复（默认空答案已发给 cc）」。R9 测试：12 schema + 4 hook-receiver + 7 ai-router AUQ prompt/parse + 9 router AUQ branch + dead-drop。R10 docs。819/819 tests pass，bundle 0.0.1 验证 OK。
- 2026-05-12 v0.0.1 tag + GitHub release：tag `v0.0.1` 指向 PR #145 合并 commit `c21b381`；GitHub release page 用自定义 release notes（核心能力清单 + 文档链接 + post-0.0.1 follow-ups）。注释格 0.0.1：155 个 commits 起 / 6 packages（shared / storage-files / im-lark / term-wezterm / cli-cc / bridge）。
- 2026-05-13 AI 分诊 prompt 第 4 次迭代 + model upgrade：
  - **prompt**：增加 **Example E**（multi-sentence work_temp real-case anchor，演示从 4 句话 + discourse marker 「那」 + 中间 3rd-person pronoun 的复杂消息里 strip routing cue + rewrite pronoun）+ **LENGTH IS NOT AN EXCUSE** 显式指令 + 列出 conversational starters (那 / OK / 好 / 那么 / Then / So) 算 routing cue 一部分。修 user 报的「那你跟 work temp 说...他改完的话」case 被 AI verbatim 全 forward 的问题。
  - **model**：`claude-haiku-4-5` → `claude-sonnet-4-6`。Haiku 4.5 在 multi-sentence + multi-topic 复杂消息上 fragile（漏 strip routing cue / 漏 rewrite 3rd-person pronouns），prompt iterate 3 轮（PR #143 / #144 / #145）后结构性 fragile 仍未根除。User 拍板 trade 1-3s 额外 latency 换 Sonnet 4.6 强 instruction-following。同步更新 ai-router.ts 注释（latency / model selection rationale）+ conventions.md AI 路由 plain IM msg 状态总表行。`DEFAULT_MODEL` export 供 unit test verify。
- 2026-05-13 v1.12 DD LOCKED（[PermissionRequest hook IM bridge DD](superpowers/specs/2026-05-13-permission-request-hook-bridge-dd.md)）：cc 内置 `checkPathSafetyForAutoEdit` 对 `.claude/` `.git/` `.vscode/` `.idea/` 等 DANGEROUS_DIRECTORIES + `.bashrc` `.zshrc` `.env`-类 DANGEROUS_FILES hard-coded `ask` gate，跑在所有 user-level allow rules **之前**，PreToolUse hook 的 `allow` 不能 bypass（cc 2.1.88 source: [filesystem.ts:1302-1338](https://github.com/orime-org/multi-cc-im/blob/main/docs/superpowers/specs/2026-05-13-permission-request-hook-bridge-dd.md#23-session-destination-bypass) 「MUST come before checking allow rules」）。唯一能 bypass 的是 `alwaysAllowRules.session`（in-memory）走 `filesystem.ts:1268-1300` step 1.6 early return。`session` destination 只能由 **PermissionRequest hook** 的 `decision.updatedPermissions` 注入（PreToolUse hook output 不支持该字段）。multi-cc-im 现状只订阅 PreToolUse + Stop，所以 IM 模式下 sensitive path 卡 cc TUI dialog 无人答 / IM 收不到。DD 全 9 维度 user-locked：D1-A 全订 PermissionRequest / D2-A `/start auto` silent emit single-yes（不偷写 session rule 保护可见性）/ D3-A `/start off` forward IM + render cc 的 `permission_suggestions` / D4 numbered + cc 原文 wording 复用 / D5-B `/start auto` 模式 IM audit log 一行 `🛡️ daemon auto-allowed cc edit to <path>` 通知不需答 / D6-A user IM 不能升级 always（保护 D2-A 一致性，只能选 cc 给的 suggestion）/ D7 协议事实 `{decision: {behavior: 'allow'|'deny', updatedInput?, updatedPermissions?}}` / D8 110s 内部 + 120s cc-side（mirror v1.9 AUQ）/ D9 assume only-hook + README 文档化 multi-hook caveat。P1-P9 实施 pending（mirror v1.7 / v1.9 cadence ~6-8 PRs）。PR #148 draft / PR (this one) lock。
- 2026-05-13 v1.12 P1-P7 全部主线落地（PR #150 P1 setup-hooks subscribe `PermissionRequest` event + shared `PermissionRequestPayload` type + payloads zod schema + hook-receiver silent stub / PR #151 P3+P4 state-files 加 `PermissionDialog{Request,Response}File` discriminated union schemas + 文件 IPC helpers + hook-receiver 真正 PermissionRequest 分支：3 步 short-circuit (IMWork null / !IMOrigin / !daemon) + 写 Request file + poll 110s + emit cc stdout JSON + 超时 fallback plain allow (no `updatedPermissions` 保护 D2-A) / PR #152 P5 orchestrator handlePermissionDialog `/start auto` silent single-yes 写 Response + D5-B IM audit log `🛡️ daemon auto-allowed cc 编辑敏感路径` + `scheduleReaper(kind: 'permission-dialog')` 加 130s 兜底 + `inferSensitivePathFromToolInput` 提路径 (file_path/path/command 优先) / PR #153 P6 ai-router 加 `renderPermissionRequestPrompt` + `parsePermissionRequestOutput` + `routePermissionRequestViaAI` + shared `PermissionDialogAnswerSchema` (discriminated union allow + optional appliedSuggestionIndex 1-based / deny + optional message) + `summarizePermissionSuggestion` 从 PermissionUpdate.rules[0].ruleContent 提 human-readable 标签 + prompt CRITICAL anti-fabrication rule (D6-A 严禁 AI 自创不在 list 里的 always-allow) / PR #154 P7 wire end-to-end: cli-cc `listPendingPermissionDialogs` + router `RouterPermissionDialogResponse` + `aiPermissionRequestRouter` 注入 + handlePlainWithAI 加 PermissionDialog 优先分支（早于 AUQ / force-perm / routing — sensitive gate 卡 cc TUI 协议层最紧急）+ `handlePermissionDialogReply` mirror AUQ pattern + `applyDialogIndexClamp` 防 AI 返回 out-of-range index + 安全默认 deny fallback (cc 不会卡死) + orchestrator `/start off` 模式 forward IM 用 `formatPermissionDialogPrompt` 渲染 numbered options (`1. 同意一次` + `N+1. 始终允许: <ruleContent>` + `N+2. 拒绝`) + `handlePermissionDialogResponseFromIM` 解析 appliedSuggestionIndex 查 Request `permissionSuggestions[index-1]` 写回 Response.updatedPermissions + dead-drop 提示「⏱ cc 已超时，本轮不再等待你的 PermissionDialog 回复」。871/871 tests pass，bundle 0.0.2 verified。**P8+P9 真账号 smoke + docs 本 PR 落地**。
- 2026-05-13 真账号 smoke fix — drop `RESERVED_BRIDGE_NAME='multi-cc-im'` silent rejection in matcher：root cause 是 matcher.ts 把 bridge package 名 `multi-cc-im` 硬编为保留字 → 用户在本仓库开发把 cc tab `/rename multi-cc-im` 后，IM 发 `#multi-cc-im /1` matcher 第二行 silent return `none` → router 报「`#multi-cc-im` not found」。`/list` 输出反而把它列为「可寻址 #multi-cc-im」造成 UX 矛盾。原假设「用户不能 legitimately /rename 成这个字符串」错误（本仓库 repo 名就是 multi-cc-im，用户起名合情合理）。修法：删除 `matcher.ts` 的 `RESERVED_BRIDGE_NAME` 常量 + line 65 守卫 + `bridge/src/index.ts` barrel 导出 + `matcher.test.ts` 旧 reserved-name 用例反过来加 positive 回归 test（query='multi-cc-im' 当作普通 tabTitle 解析）。bare-slash 命令（`/list` `/start`）跟 `#<tab>` 引用走 parser 不同分支，命名空间不会撞。875/875 tests pass，bundle 0.0.2 verified。
- 2026-05-13 v0.0.3 tag + GitHub release：lockstep version bump 跨 8 个 package.json（root + apps/multi-cc-im + 6 workspace packages）+ `cli.ts` VERSION 常量。0.0.3 since 0.0.2 (PR #147)：v1.12 PermissionRequest hook IM bridge 完整链路落地 — PR #148 DD draft / #149 DD lock / #150 P1 subscribe event / #151 P3+P4 state-files + hook-receiver real handler / #152 P5 `/start auto` silent allow + IM audit / #153 P6 ai-router prompt + parser + caller / #154 P7 wire end-to-end + `/start off` numbered IM forward / #155 P9 docs / 真账号 smoke 暴露 2 fix #156 `#<tab> /1 /2` rigid-syntax 也答 PermissionDialog pending + #157 drop `RESERVED_BRIDGE_NAME` silent rejection。`pnpm install --frozen-lockfile` 通过（version bump 不改 resolution graph），875/875 tests pass，bin smoke 输出 0.0.3。
- 2026-05-13 v1.13 DD LOCKED（[iTerm2 term adapter DD](superpowers/specs/2026-05-13-iterm2-adapter-dd.md)）：iTerm2 作为第二个 `TermAdapter` 接入。Fact-finding 锁定 6 个候选（C0 不做 / C1 Python API + 临时 helper subprocess / C2 Python API + 持久 daemon-side Python / C3 AppleScript only / C4 `it2` CLI wrapper / C5 直接 WebSocket+protobuf）。**Critical gap：iTerm2 没有 `WEZTERM_PANE` 等价**，仅 `ITERM_SESSION_ID = "w0t1p0:UUID"` 自动注入子进程；位置部分 `w0t1p0` 不稳定（用户关 pane 编号会移），只有 UUID 部分稳定 → cli-cc hook entry gate 必须从硬读 wezterm env 抽象为 per-terminal 可插，`PaneId` 类型从 `number` 扩到 `number | string`（branded union）。**用户 3-yes 预拍板**（Python 依赖 OK / PaneId string OK / iTerm2 偏好+macOS 权限 UX OK）→ 推荐 **C1**：mirror cli-cc 的「one ephemeral subprocess per hook event」模型，最小架构入侵，每 call 多 ~100-300ms latency 可接受。砍 C3/C4/C5（capability gap / CLAUDE.md "用现有 SDK 不造轮子" / 协议无 docs）；C2 留 v2 push lifecycle events 用。P1-P7 实施 pending（mirror v1.12 cadence）：P1 PaneId 类型 + cli-cc entry gate 抽象 / P2 `packages/term-iterm2/` adapter + Python helper / P3 `python3` path-resolver + 安装检查 / P4 `start iterm2` setup wizard + ANSI 配置指南 / P5 orchestrator wiring / P6 docs / P7 真账号 iterm2 跑 cc smoke。
- 2026-05-13 v1.13 P1 实施落地（PR #162）：纯 refactor，无行为变化（WezTerm detector 仍是唯一注册的 detector）。`packages/shared/src/types.ts` `PaneId` brand 扩 `Brand<number | string, 'PaneId'>` + `PaneIdSchema` 走 `z.union([z.number().int().nonneg(), z.string().min(1)])`。新 `packages/cli-cc/src/pane-id-detectors.ts` 含 `PaneIdDetector` 类型 + `runDetectors` helper + `DEFAULT_DETECTORS = [detectWezTermPaneId]` 列表（iTerm2 detector P2 加）。`hook-receiver.ts` `defaultResolvePaneId` 改委托给 `runDetectors(DEFAULT_DETECTORS, process.env)`。`state-files.ts` 所有 helper + filename parser carry `PaneId`，regex 第一捕获组 `(\d+)` 扩 `([A-Za-z0-9-]+)` 接受 WezTerm numerics + iTerm2 UUIDs，新 `brandPaneIdFromFilename` 保证 WezTerm 数字字符串 round-trip 回 `number`（反向路由 Map key 不破）。跨包 `paneId: number` → `paneId: PaneId` ×多处（`shared/adapter/cli.ts` `bridge/orchestrator.ts` `cli-cc/adapter.ts` `apps/multi-cc-im/hook.ts`），删除 `as unknown as number` 冗余 cast。新 `pane-id-detectors.test.ts` 12 cases。888/888 tests pass，bundle 0.0.3 verified。
- 2026-05-13 v1.13 P3 实施落地（PR #164）：iTerm2 daemon startup 接入的 infra 准备（**不接通 daemon，那是 P5**）。`packages/shared/src/adapter/storage.ts` `ExternalPathsSchema` 加 `python3?: string` field（mirror 已有 `wezterm` / `claude` —— 给 P4 wizard 写入 + P5 startup 读取用）。`apps/multi-cc-im/tsup.config.ts` 加 `onSuccess` 函数式 callback 把 `packages/term-iterm2/bin/iterm2-helper.py` 复制到 `dist/iterm2-helper.py`（**踩坑**：tsup string-form onSuccess 会把 `../../` 路径段吃掉变成 `packages/...` → ENOENT，改用 function form + `import.meta.url` + `path.resolve` 自己算路径）。`apps/multi-cc-im/src/start.ts` 新增两个 export：`defaultResolvePython3(cachedPath?)` mirror `defaultResolveWezTerm` 的 cache-then-fallback 模式 + `resolveIterm2HelperPath()` 通过 `import.meta.url` 在 bundled (`dist/iterm2-helper.py`) 和 dev tree (`packages/term-iterm2/bin/iterm2-helper.py`，相对 src 退三级到 repo root) 两个位置探测 helper.py，throw with install hint if neither. `storage-schemas.test.ts` 加 4 新 case (python3 accept/reject + all-three combo)，`start.test.ts` 加 3 新 case（defaultResolvePython3 smoke + cached round-trip + resolveIterm2HelperPath 两 branch）。939/939 tests pass，bundle 0.0.3 verified（258.69 KB，`dist/iterm2-helper.py` 5.8 KB 复制 OK）。`pnpm install --frozen-lockfile` 通过（无 package.json 变化）。
- 2026-05-14 v1.13 P6 docs（PR #167）：用户文档同步 iTerm2 接入。`README.md` + `README.zh-CN.md` 同步改: ① Prerequisites 加 iTerm2 ≥ 3.3 alternative 二选一 + 标 "macOS only / 需 Python 3 + 一次性偏好 + Automation 权限" / ② First-time start 描述 wizard 先选 term 再选 IM 两步 + persist 路径 / ③ Just-talk + Control daemon 章节都把 wezterm 替换 / 加 `/start` echo 含 `✓ terminal: <id>` 行说明 / ④ Where things live 加 `[terminal] type` 跟 `[external_paths] python3` + `dist/iterm2-helper.py` / ⑤ Troubleshooting 加 3 新条 (`python3 not found` / `cannot import iterm2` / iTerm2 tab title 在 /list 看不到)。DD doc (`2026-05-13-iterm2-adapter-dd.md`) status banner 标 P1-P5 IMPLEMENTED + § 9 milestone list 每条加 ✅ + PR 号 + 实施 notes（P4+P5 合并理由 + P3 tsup string-form 踩坑链接）。无代码变更（pure docs PR）。剩 P7 真账号 smoke。
- 2026-05-14 v1.13 P4 + P5 合并实施落地（PR #166）：iTerm2 setup wizard + orchestrator wiring 一起到一个 PR（P5 description「expected to be no-op — adapter is plug-compatible」与 P4 scope 自然内聚，避免中间 PR 半可用状态）。**用户拍板的 wizard 流程 (D 方案)**：`multi-cc-im start` 无 arg → ① 先选 terminal（wezterm 或 iterm2）→ ② 再选 IM（lark）→ 启 daemon。IM 端 `/start [auto/off]` 回包加 `✓ terminal: <id>` 行让用户从 IM 侧确认 daemon 用了哪个 term。变更：`packages/shared/src/adapter/storage.ts` 加 `TerminalIdSchema` (`'wezterm' \| 'iterm2'`) + `TerminalConfigSchema` + `ConfigSchema.terminal` field（default `'wezterm'` 后向兼容；旧 config 缺该 section 当 wezterm 解读）。新 `apps/multi-cc-im/src/terminal-selector.ts` interactive `selectTerminal` wizard：选 iterm2 → render ANSI hyperlink 引导用户开 iTerm2 Python API 偏好 + `confirm` prefs done → resolve python3 → prompt `pip install --user iterm2` → smoke `python3 -c "import iterm2"`。`start.ts` 重排流程：§0a selectTerminal 先 + 持久化 `[terminal].type` + (iterm2) `[external_paths].python3`；§1b 改 conditional：wezterm 走原 `resolveWezTerm` path，iterm2 走 `defaultResolvePython3` + `resolveIterm2HelperPath` (P3 infra)；adapter creation 改 conditional `createWezTermAdapter` vs `createITerm2Adapter`（P5 wiring）；wezterm-only `livePanes` pre-flight + sweep `livePaneIds` 加 `termId === 'wezterm'` guard。`bridge/router.ts` `RouterOpts.terminalId?: 'wezterm' \| 'iterm2'` + `handleBridgeCommand` 第 7 个 param + `/start` echo 加 `✓ terminal: <id>` 行（不传时不渲染保持 backward compat）。`bridge/orchestrator.ts` route() 调用读 `opts.termAdapter.name === 'iterm2' ? 'iterm2' : 'wezterm'` 传 router. 新 `apps/multi-cc-im/src/terminal-selector.test.ts` 7 case + `start.test.ts` `stubSelectTerminal` + 14 callsites 注入 + storage-schemas.test.ts +6 case + router.test.ts +3 case。955/955 tests pass，bundle 0.0.3 verified（271 KB），`pnpm install --frozen-lockfile` 通过。剩 P6 docs + P7 真账号 smoke。
- 2026-05-14 v1.13 P7 真账号 smoke 3 fix（PR #168）：用户在 iTerm2 真账号跑 wizard 暴露 3 UX/兼容性问题。①「Did you enable Python API in iTerm2 Preferences?」prompt 默认 No 不合理（用户开 prefs 后再来 wizard，应该一回车确认）→ `initialValue: true`。② `pip install --user iterm2` 在 macOS Homebrew Python ≥ 3.12 上撞 PEP 668 `externally-managed-environment` 失败 → 加 `--break-system-packages` flag（配合 `--user` 仍在用户 site-packages 不污染 system，brew Python 仍干净），错误信息加 pipx fallback hint。③ select prompt 不知道终端有没装就让用户选 → `SelectTerminalDeps` 加 `detectWezTermInstalled` + `detectIterm2Installed` 两个 deps（default: wezterm 走 `resolveWezTermPath` 尝试；iterm2 检查 `/Applications/iTerm.app` + `~/Applications/iTerm.app`），option `hint` 体现 `✓ installed` 或 `not installed — brew install --cask <name>`。新 `baseDeps()` test helper 集中化 spread defaults；新 3 case 验证 (installed/not-installed hint + `--break-system-packages` arg presence)。958/958 tests pass，bundle 272 KB，frozen-lockfile 通过。
- 2026-05-14 v1.13 P7 follow-up gate on not-installed（PR #169）：用户跑 PR #168 后发现 select prompt 仍允许选未安装的 term — hint 只是"提示"，没拦住。改 `selectTerminal` 在 `confirm` 选项前增加 hard gate：选中的 term 若 `detectXInstalled() === false`，throw `Error: <id> is not installed. Run: brew install --cask <pkg>` 终止 wizard，daemon 不启动；安装好再 `multi-cc-im start` 即可。新 2 test case 验证 (selected-not-installed 抛错 + selected-installed 通过)。**根因**：PR #168 只做"hint 渲染"未做"gate"，是治标。修法补 gate 是治本。959/959 tests pass，bundle 272 KB。
- 2026-05-14 v1.13 P7 follow-up mdfind detection（PR #170）：用户提到本地装了 3 个 iTerm，PR #168/#169 只检查 `/Applications/iTerm.app` + `~/Applications/iTerm.app` 两个硬编码路径，错过用户自定义路径的安装（"开源项目用户安装位置不一" — CLAUDE.md 编码纪律）。改用 macOS Spotlight `mdfind kMDItemCFBundleIdentifier == <bundle-id>` 探测，bundle-ID 路径无关：`com.googlecode.iterm2` 找 iTerm2，`org.wez.wezterm` 找 WezTerm。新 `mdfindByBundleId` helper + 12 test case（mdfind 命中/未命中/stderr/超时/wezterm 路径同样改造）。**根因**：之前的 path-list-detection 是不可扩展的硬编码 list，治标；mdfind 是 macOS native 安装注册表查询，治本。959/959 tests pass，bundle 272 KB。
- 2026-05-14 v1.13 P7 follow-up daemon.log 诊断日志（PR #171）：用户真账号 smoke 出问题时 stderr 信息有限，看不出 daemon 内部哪一步卡了。加 `~/.multi-cc-im/daemon.log` dual-write logger（stderr 流式 + 文件 append-only，0600 mode），launch banner `=== daemon started PID=N root=X ===` / stop `=== daemon stopped PID=N ===`。**两套 sink** 区分公开度：`log` 走 stderr + file 给用户看大事件（lark 连接 / 注册等）；`fileOnlyLog` 只写文件，给 AI 看 iterm2-helper 子进程每次 invocation 的详细 trace（用户不需要看 `[iterm2-helper] action=listSessions (timeout=8000ms)` 这种噪音）。iterm2-helper.py 加 stderr `_log(msg)` 进度打点（`start action=X / iterm2 module imported / opening WebSocket / app obtained, windows=N / WebSocket closed cleanly`），`python-bridge.ts` 流式转发 child.stderr 到 `log` callback + 隐私 hygiene `summarizeRequest`（UUID 截 8 char、sendText `textLen=N` 不打印内容）。`AppPaths.daemonLog` field 新增；`config-paths.ts` 计算 `<root>/daemon.log`。README + README.zh-CN 加「`tail -f ~/.multi-cc-im/daemon.log` 看 daemon 运行轨迹」一行。961/961 tests pass，bundle 277 KB。**注意**：daemon.log 用 0600 但不应含 secret；argv / 路径仍可能泄露 host 信息，按 credential 同级谨慎对待。
- 2026-05-14 lark WS 重连策略改写（PR #172）：替换 `@larksuiteoapi/node-sdk` 默认指数 backoff (1s→2s→4s→8s→…) 为固定间隔重试循环 — 失败 1s 后重连，连 10 次失败冷却 5s 后继续循环，永不放弃。**根因**：真账号 smoke 暴露 SDK 内部 backoff 不暴露 hook，最长可达 4 分钟无连接 + stderr 静默，用户/AI 都看不出"在重连还是已死"。变更：`packages/im-lark/src/adapter.ts` `CreateLarkAdapterOpts` 加 `retryIntervalMs` / `cooldownMs` / `cooldownAfter` 测试 seam（prod 默认 `1000`/`5000`/`10`），WSClient 配置 `autoReconnect: false`，新增 closure-scope `stopRequested` + `retryTimer` 状态 + `tryConnect()` / `scheduleRetry()` 互递归；`onReady` callback 设 `connected=true` 区分首连接/重连日志（`WS connected (after N attempt(s))` vs `WS reconnected — bridge ready`），`onError` 触发 `scheduleRetry()` 走自管路径；`stop()` 先 flip `stopRequested` + `clearTimeout(retryTimer)` 再 close wsClient。`adapter.test.ts` 改 2 既有用例（首连接日志 prefix-match 新 attempt-count suffix；onReconnecting → 改测 "onError + onReady 触发 reconnected 日志"），加 1 新用例验证 10 失败冷却 + 循环继续（用 seam 10ms/50ms/10 在 ~100ms 内跑完）。961/961 tests pass，bundle 0.0.3 verified（277 KB），bin smoke 输出 `0.0.3`。**真账号 smoke 待跑**：等用户在新 retry loop 下确认 "连接中... → WS connected (after N attempt(s))" 日志能流出来取代 4 分钟静默 backoff。
- 2026-05-14 v1.13 P7 follow-up wizard 真连接验证（PR #174）：iTerm2 wizard 把"自报 + import smoke" 改成"empirical connect smoke"。**根因**：PR #166 的 `confirm("Did you enable Python API?")` 是 hope-not-gate — 用户按 Y 时 wizard 不能核实，按完进入下一步；下游 `import iterm2` smoke 只验证 PyPI 包能 import，**不验证 API server 是否真在跑**。结果：用户没勾偏好 / 勾了但 iTerm 没运行 / cookie 路径异常时，wizard 全放行，daemon 启动后第一次 listSessions 才在 helper.py 报 `There was a problem connecting to iTerm2`（2026-05-14 真账号 smoke 现场症状）。变更：`apps/multi-cc-im/src/terminal-selector.ts` 删 `prefsDone` confirm prompt + 相关 cancellation 分支；用户指引保留为 io.message（仍告诉用户去 Settings 哪里勾）；smoke 改成 `python3 -c "import iterm2; async def main(c): await iterm2.async_get_app(c); iterm2.run_until_complete(main)"` — `run_until_complete` 失败即说明 API server 不可达，错误信息 surface 时附 "Settings → General → Magic → ☑ Enable Python API" 三步指引 + iterm2 lib 原始错误。`io.message` 文案微调（"Preferences" → "Settings"，加第三步说明 wizard 会自己验证）。`terminal-selector.test.ts` 删 "user declines pref enable" case（prompt 没了），改名 "happy path" 用例验证两次 exec（pip install + connect smoke）+ 第二次 args 含 `iterm2.run_until_complete` / `async_get_app` substring；改名 "import smoke fails" → "connect smoke fails (API off)" 验证错误 message 含 "Settings → General → Magic" / "Enable Python API" / "There was a problem connecting to iTerm2" 三段；"python3 missing" 用例 confirms 队列清空（python3 解析现在在唯一 confirm 之前）；"PEP 668 flag" 用例改成单 confirm。README + README.zh-CN 替换 "cannot import iterm2" troubleshoot 为 "cannot connect to iTerm2 Python API"，加两个根因（偏好 off / iTerm 没跑）+ pip 缺包后备方案；"`/list` 看不到 tab title" 块改成"connect smoke 已验证偏好，仍有问题=Automation 权限被撤"。无 package.json 改动，960/960 tests pass（-1：删了 prompt 用例），bundle 0.0.3 verified（277 KB），bin smoke 输出 `0.0.3`。**未做**：3 iTerm 共存 race（哪一份 iTerm 写 cookie 决定 helper 连谁）作为已知 corner case 留待之后专门处理。
- 2026-05-14 IMWork → 每终端 IMWezterm + IMIterm2（PR #175，issue 378）：cc hook scripts 用户全局装在 `~/.claude/settings.json`，所有 cc 实例都 fire `multi-cc-im hook ...`。原 `<stateDir>/IMWork` 单文件 = master IM-mode 开关，不区分终端 → daemon 跑 iterm2 模式时，wezterm cc 的 Stop hook 进 daemon 仍被当 IM 模式开启来处理 → 把 wezterm pane 里 cc 跟用户的对话原文 forward 到 IM bot（2026-05-14 真账号 smoke 现场实锤：`[cc → IM] (pane 6) reply='本轮只是回答用户的...'` 把 wezterm 里 Claude Code 跟用户的开发对话原话发进 IM）。**根因**：「IM-mode 开关」缺少终端维度；hook 解析出 paneId 后没有携带"来自哪个终端"这一事实信息，daemon 也没用此事实做 gate。**修法**：`pane-id-detectors.ts` 每个 detector 包成 `TaggedDetector = {termId, detect}`，`runDetectors` 返回 `PaneOrigin = {termId, paneId}`。`state-files.ts` 单文件 `IMWork` 拆为 `IMWezterm` + `IMIterm2`：`imWorkFileName(termId)` helper 算文件名；`writeIMWorkFile / readIMWorkFile / existsIMWorkFile / deleteIMWorkFile` 全加 `termId` 参数；`StopFile` schema 加 `termId?: TerminalId` 字段（optional 仅为升级期 back-compat，新写永远带）；`writeStopFile` 强制要求 `termId` 参数。`hook-receiver.ts` 入口 `resolvePaneId` 改名 `resolvePaneOrigin`（type 也变），检测出 `{termId, paneId}` 后 IMWork 三处 gate 全用 `termId` 选 IM 文件 + Stop 文件写入带 `termId`。`cli-cc/adapter.ts` Stop 调度从 file body 读 `termId` 转发到 `onStop` payload，`shared/adapter/cli.ts` `CLIHandler.onStop` 签名加 `termId?: TerminalId`。`bridge/orchestrator.ts` 顶部 hoist `activeTerminalId`（从 `opts.termAdapter.name` 推），handleStop 用 payload 携带的 `termId` 查对应 `IM<TermType>` 文件，handleInbound + permission-dialog handler 都改读 `activeTerminalId` 的文件，`/start auto`+`/stop` 只写/删 active term 的文件，shutdown delete 同。**为什么不用 `typeof paneId` 推 termId**：用户明确反对——结果类型反推来源是 hack，未来加 kitty/alacritty 用 number/UUID id 就跟 wezterm/iterm2 撞了；端到端传递事实信息是 robust 设计。`apps/multi-cc-im/src/start.ts` startup 重置 IM 模式时删两个文件 (`IMWezterm` + `IMIterm2`)，`state-sweep.ts` 两个文件名都保护不扫。测试面 ~140 处用 sed 批量改 `writeIMWorkFile(stateDir)` → `writeIMWorkFile(stateDir, 'wezterm')` 等（test 默认 wezterm context 保持原假设），`pane-id-detectors.test.ts` 7 处 detector 用例重写适配 TaggedDetector，`orchestrator.test.ts` 2 处 unlink 'IMWork' 改 'IMWezterm'；orchestrator 日志 surface 保留 `IMWork off` 子串（加 `(IMWezterm)` 后缀）以兼容 log-grep 类断言。960/960 tests pass（与之前 961 比 -1 是某个 pre-test 数字差），bundle 0.0.3 verified（278.76 KB），bin smoke `0.0.3`，无 package.json 变化。**未做（issue 377 仍 open）**：iTerm cc reply 没回到 IM 的反向链路问题——`hook-receiver.ts` 入口 silent-exit if `paneOrigin === undefined`，怀疑 iTerm cc 启动时 shell 没继承 `ITERM_SESSION_ID`（待用户在 iTerm cc TUI 里跑 `echo $ITERM_SESSION_ID` 验证）。
- 2026-05-14 hook gate-level diag trace（PR #178，issue 377 follow-up）：PR #177 入口 trace 证实 cc-fired iTerm Stop hook **真的 fire 了** 且 env 完好，但 Stop 文件不写 / daemon 不响应；手动用同样条件复现 → 一切正常 → silent-exit 卡在 receiver 内部某个 gate / parse 失败 / writeStopFile 抛错，但旧 trace 没法看清楚到底哪一步。修法：① `RunHookReceiverOpts` 加 `trace?: (line: string) => void` callback（错误 swallow，永不 break hook）。② `runHookReceiver` 在 detector 落空 / detect 出来 / Stop branch 每个 gate (`stop-gate IMWezterm=true/false` / `stop-gate IMOrigin=...` / `stop-gate daemon-alive=...`) / pre-write (`stop-write paneId=... sid=... ts=... msg-len=N stop_hook_active=...`) / post-write (`stop-write OK`) 都打 trace line。③ `apps/multi-cc-im/src/hook.ts` 把 trace 写盘统一进 `appendTraceLine(traceLogPath, line)` helper；entry trace 复用；新增 parse-fail trace (含 stdin 头 200 字节 escape 后 echo) + receiver-throw trace。④ buffering: receiver 的 trace callback 同步入队，receiver 返回/抛错后一次性 async flush 到磁盘（避免 each callback 都 await）。新 2 测试覆盖（Stop branch 6 行 gate trace + parse-fail 含 stdin-head）；既有 entry-trace 测试改成 `lines.length >= 1`。965/965 tests pass，bundle 0.0.3 verified (281.86 KB)，bin smoke `0.0.3`，无 package.json 变化。**预期**：用户跑一轮 iTerm cc 回复 → hook-trace.log 必有这 6 行之一被 trace 到，定位 silent-exit 精确点。**验证：trace 直接抓到 `stop-gate daemon-alive=false` —— 根因锁定 isDaemonAlive，详见 PR #179**。
- 2026-05-14 hook 入口诊断 trace（PR #177，issue 377）：诊断 iTerm cc reply 不回 IM 的 bottleneck —— 已验证 hook 代码路径 (env 给对就写文件) + daemon watcher (UUID 命名 Stop 文件能 forward) 两端都正常，但 cc 实际触发 Stop hook 时 daemon 完全收不到信号。差「cc 到底有没有 invoke hook command + invoke 时 env 是啥」这一不可观察环节。修法：`apps/multi-cc-im/src/hook.ts` 在 `runHookCommand` 入口（**所有** parse / detector / IMWork gate 之前）无条件 append 一行到 `~/.multi-cc-im/hook-trace.log`：ISO ts + event (从 CLI argv 拿，不靠 stdin parse) + pid + ITERM_SESSION_ID + WEZTERM_PANE + stdin-bytes。失败 swallow（best-effort 诊断不能挂 cc turn）。`apps/multi-cc-im/src/cli.ts` `dispatchHook` 转发 `args[0]` 当作 event 给 `runHookCommand`（之前 void 掉了）。新 opt `traceLogPath?: string | null`：默认 `<dirname(stateDir)>/hook-trace.log`，tests 传 `null` 关掉。新 3 测试覆盖（trace 写入格式 + 空 stdin 也写 + null 关闭分支）；10 个既有用例加 `traceLogPath: null` 防污染 tmp。963/963 tests pass，bundle 0.0.3 verified (279.86 KB)，bin smoke `0.0.3`，无 package.json 变化。**预期下一步**：用户跑 daemon + 在 iTerm cc 触发一轮回复 → 查 trace 文件：① 有行 + env 含 ITERM_SESSION_ID → bug 在 detector 之后；② 有行 + env 空 ITERM_SESSION_ID → cc 剥光 env；③ 无任何行 → cc 在 iTerm 根本没 fire Stop hook。
- 2026-05-14 iterm2-helper stderr 静音（PR #176，issue 380）：用户 PR #175 真账号 smoke 后反馈：`[iterm2-helper] action=listSessions / sendText / sendKeystroke` 每个 IM 动作打 3+ 行，daemon 控制台被淹。**根因**：PR #171 wiring iterm2 adapter 的 log 走 `log` sink (stderr + 文件双写)，无区分高音量 trace 跟用户级事件。修法：`apps/multi-cc-im/src/start.ts` 加 `fileOnlyLog` sink (file only, opts.log 注入时 fallback no-op 保测试干净)；`createITerm2Adapter({ log: fileOnlyLog })` 取代 `log`，trace 仍落 `~/.multi-cc-im/daemon.log` 给 AI post-mortem 用，stderr 干净。无 schema / 接口变更，960/960 tests pass，bundle 0.0.3 verified (279 KB)，bin smoke `0.0.3`。
- 2026-05-14 issue 377 根因修复 — captureProcessLstart 强制 LC_TIME=C（PR #179）：iTerm cc reply 不回 IM 的 root cause 锁定。PR #177 + #178 两轮 trace 把 silent-exit 精确定位到 `isDaemonAlive` 返回 false，但 daemon 当时明明在跑、daemon.pid 内容正确、`process.kill(pid, 0)` 成功。**根因**：`captureProcessLstart` 调 `execFile('ps', ['-o', 'lstart=', '-p', pid])`，ps 输出的 lstart 字符串 **locale-dependent**（en_US: `Thu May 14 19:16:33 2026` / zh_CN: `四 5月/14 19:16:33 2026`）。daemon 在用户 shell (`LC_TIME=en_US.UTF-8`) 下启动 → 存 en 字符串到 daemon.pid。cc 给 Stop hook 子进程的 env 里 `LC_TIME` 不同（或缺失 → ps 默认）→ ps 返回不同格式 → 字符串比较失败 → `isDaemonAlive=false` → silent skip → cc 回复不 forward 到 IM。**修法**：`packages/cli-cc/src/state-files.ts` `captureProcessLstart` 在 execFile env 注入 `LC_TIME=C` + `LC_ALL=C`（POSIX locale）→ ps 输出永远是 ASCII 英文格式，无论调用方 locale。daemon 写盘 + hook 读盘两侧都走 C locale → 字符串比较确定性匹配。`process.env` 浅 merge 保留 PATH 等其他 env（ps 在 PATH 上，env-strip 会找不到）。新 1 test case 验证：set `LC_TIME=zh_CN.UTF-8 LC_ALL=zh_CN.UTF-8 LANG=zh_CN.UTF-8` 后调 captureProcessLstart 仍返回 ASCII 英文月份字符串（与 baseline 完全一致）。**为什么不丢 lstart 检查只看 process.kill(pid,0)**：lstart 检查防 PID 重用（OS 杀 daemon + 别人拿到同 PID 我们误以为 daemon 还活着）。本地 daemon 场景 race 极小，但保留这道防线无成本（修了 locale bug 后字符串比较仍然是确定性的）。964/964 tests pass，bundle 0.0.3 verified (279.93 KB)，bin smoke `0.0.3`，无 package.json 变化。**预期**：PR merge + 重启 daemon + 再测一次 iTerm cc 回复 → IM 应收到。PR #177 + #178 的诊断 trace 留着，debug 万一再有问题省事，后续可单独清理。
- 2026-05-15 `docs/architecture.md` 整版重写到 v0.1.0（PR pending）：旧文 ~400 行是 v1.4 wechat 时代留下的，散布 `im-wechat / iLink / openclaw / Tencent` 字样，只在 header 加了历史警告做最低限度同步。本次整版重写覆盖：① 技术栈刷新（Lark `@larksuiteoapi/node-sdk` + iTerm2 PyPI iterm2 包 + Node 22+ ESM）；② 4 维 adapter 架构图替换 `wechat → lark` + `wezterm → wezterm/iterm2` + 唯一例外注明 daemon 自起 `claude --print` 子进程做 AI 路由 triage；③ 包依赖图删 im-wechat / openclaw，加 term-iterm2（spawn python3 + helper.py）；④ adapter 对照表 v0.1.0 实现列重写；⑤ 目录结构反映现状（含 dist/iterm2-helper.py、shared/PaneId Brand union、cli-cc/PaneOrigin detector chain）；⑥ 数据存储 schema：`wechat-cursor` → `lark-cursor`、`IMWork` 拆 `IMWezterm` + `IMIterm2`（issue 378）、Stop file body 加 `termId` 字段、daemon.pid 的 `startedAt` LC_TIME=C 锁定（issue 377）、新增 `daemon.log` + `hook-trace.log`（env-gated by `MULTI_CC_IM_DEBUG`）；⑦ 关键数据流三段全部重写：Inbound IM (lark)、Outbound cc Stop → IM、PreToolUse → IM 审批（detector chain 解释 + IM<termId> 文件 gate）；⑧ 新增「PaneOrigin / TerminalId 端到端传递」section 解释 issue 378 根因 framing（禁止 `typeof paneId` 反推）；⑨ 外部 CLI 工具策略加 python3 列 + iTerm2 Python API 偏好 empirical verification；⑩ Pane 活性策略改造为「wezterm cli list 或 iTerm2 helper listSessions 取 ground truth」；⑪ iLink 韧性 section 完全替换为「Lark WS 韧性 (PR #172 改写)」覆盖 driver-side 1s 重试 + 10 次冷却 + 永不放弃逻辑；⑫ `isDaemonAlive` 实现段加 LC_TIME=C 锁定细节 + memory pointer；⑬ 多机部署约束 rationale 从 iLink getupdates cursor 改成 lark / 任何 IM 全局 cursor + cc 终端 env 识别 + daemon.pid 双开检测。新文 443 行，无代码改动，typecheck 8 项绿。**Follow-up 不在此 PR**：DD 报告交叉引用准确性二次审计（指向 superpowers/specs/ 下的 DD 文件路径假设 hash-stable）。
- 2026-05-15 cc monitor dashboard 实施落地（issue 390）：DD 全 5 步用户拍板 `A2 hono / B0 纯内存 / C1 SSR + meta refresh 5s / D1 port 40719`，[DD spec](superpowers/specs/2026-05-15-cc-monitor-dashboard-dd.md)。新 workspace package `@multi-cc-im/monitor`（`packages/monitor/`）8 文件 + 4 test 文件 40 case：`prices.ts` vendored LiteLLM Claude 4.x 子集（opus-4-1/4-5/4-6/4-7 + sonnet-4-5/4-6 + haiku-4-5，flat `cache_creation_input_token_cost`，frozen const + `priceForModel` 长 key 优先 fuzzy match + `computeUsd` 4 token 类 linear sum）；`cost.ts` `computeSessionCost` tail jsonl 聚合**所有** `message.usage` blocks（不是只末条）+ skip malformed lines + `findRecentSessions` mtime sort；`metrics.ts` `ErrorRingBuffer` FIFO 容量 200（capacity clamp `Math.max(1)`、defensive copy `snapshot()`、time injection seam）；`render.tsx` JSX 隔离 + `String(tree)` 边界返回 HTML；`views/{layout,dashboard,daemon-state,sessions-table,errors-table,cost-table}.tsx` 可组合 SSR 视图，`<meta http-equiv="refresh" content="5">` + `color-scheme: light dark`；`index.ts` `startMonitor()` hono app + 5 路由（`/` SSR / `/health` / `/api/{state,sessions,errors,cost}`）+ `MonitorHandle.{port,url,stop}`，`serve()` 包 Promise 等 listen 完成才 resolve（防 race）。`apps/multi-cc-im/src/start.ts` §3b 在 orchestrator.start() 后调 `startMonitor()`，binding 失败 swallow + 提示 port 冲突（daemon 继续跑 IM bridge）；orchestrator `onError` callback mirror `errorBuffer.push(phase, msg)`；shutdown 序里 monitor.stop() 在 orchestrator.stop() 之前（防 /api/sessions slow path 跟 termAdapter teardown 抢）。`apps/multi-cc-im/tsconfig.json` 加 `jsx: "react-jsx" + jsxImportSource: "hono/jsx"` —— consumer typecheck 会 transitive walk 进 `render.tsx`，启 JSX 才不报 TS6142（base tsconfig 不动，影响范围限本 app）。`pnpm-lock.yaml` 加 `@multi-cc-im/monitor` workspace 链 + hono@^4.12.18 + `@hono/node-server@^2.0.2` 真依赖。**B 数据范围用户砍**：流量计数器（IM in/out + hook fire 分类）不要、时序数据（持久化 + 历史趋势）不要 — 只留 4 类 real-time data → 顺势 lock B0 纯内存。`pnpm install --frozen-lockfile` 通过，1011/1011 tests pass（+40 monitor），typecheck 9 项绿（含新 monitor 包），bundle 0.1.0 verified（421.23 KB，从 282 KB 涨到 421 KB = +139 KB hono runtime + http server，可接受），bin smoke `0.1.0`。**Drive-by finding**（独立 follow-up）：`conventions.md /usage /cost 段` field schema 过期（写的是 `cache_creation.ephemeral_5m/1h_input_tokens` 拆分，cc 2.1.88 实测落是 flat `cache_creation_input_tokens`），不在本 PR 范围。
- 2026-05-15 hook diag trace env-gate（PR #184，issue 377 follow-up）：PR #177 + #178 留下的 `hook-trace.log` 在 issue 377 已被 PR #179 根因修后变成 dead-write —— 每个 hook 子进程仍追加一行没人读、文件无限增长。**用户拍板**：env-gate 默认静默。修法：`apps/multi-cc-im/src/hook.ts` 加 `isHookTraceEnabled()` 检查 `MULTI_CC_IM_DEBUG` env（非空 + 非 `'0'`）；`runHookCommand` 默认 `traceLogPath` 解析改为「env 未 set → null（静默）/ env set → `<root>/hook-trace.log`」。`traceLogPath` opt 仍支持 `null` 显式禁用 / 自定义路径覆盖（tests 走这条路）。新 3 测试覆盖（默认静默 / DEBUG=1 写盘 / DEBUG=0 视为禁用）。`README.md` + `README.zh-CN.md` 「文件在哪里」section 加 `daemon.log` 跟 `hook-trace.log` 两条说明，明确 hook-trace 的 env 触发条件 + 用法（"启动 daemon 跟 cc 同一 shell 里 export"）。**怎么用**：`export MULTI_CC_IM_DEBUG=1 && ./bin/multi-cc-im start` + 在同 shell 里 `claude` 启动 cc，让 hook 子进程继承 env 才看得到 trace。971/971 tests pass (+3)，bundle 0.1.0 verified (282.16 KB)，bin smoke `0.1.0`，无 package.json 变化。
- 2026-05-15 `/list` header label 跟 active terminal 走（PR #183，issue 376）：v0.1.0 之前 `/list` 输出顶头硬编码 `当前 wezterm tabs:`；v0.1.0 iTerm2 落地后 daemon 跑 iterm2 模式 `/list` 出来还是「wezterm tabs」字面，UUID pane 列下面对不上。修法：`packages/bridge/src/router.ts` `formatSessionInventory` 加 `termLabel: string = 'wezterm'` 参数（默认值保 back-compat），heading 改 `当前 ${termLabel} tabs:`、空态行同改。两个 callsite（`/list` + `/start` echo）从已有的 `terminalId` 参数（PR #166 P5 wiring 时已透过来）传过去。`router.test.ts` 加 2 新用例（terminalId=iterm2 头 "iterm2 tabs" / 省略 fall back 'wezterm tabs'），既有 'wezterm tabs' 用例显式加 `terminalId: 'wezterm'` 防默认值变化掉测试。`/help` 文案里「列当前 wezterm tabs」改成「列当前终端 tabs」中性表述。968/968 tests pass，bundle 0.1.0 verified (282.01 KB)，bin smoke `0.1.0`，无 package.json 变化。
- 2026-05-15 加 MIT LICENSE 文件 + `"license": "MIT"` 字段补全（PR #182）：用户发现 `README.md` 跟 `README.zh-CN.md` 的「License」section 都指向 `LICENSE` 文件但仓库根目录其实没这个文件，悬空链接。补：① 新建 `LICENSE` 用标准 MIT 模板，copyright holder 写 `orime-org and contributors`（与 GitHub org 一致 + 留 contributor 入口），② 9 个 package.json（root + apps/multi-cc-im + 7 workspace packages）加 `"license": "MIT"` 字段（虽然 `private: true` 不发布 npm，但 license 字段是 OSS 合规扫描工具的常规要求 + 显式声明意图）。`pnpm install --frozen-lockfile` 通过（license 字段不影响 resolution graph），typecheck 8 项绿。**仓库 LICENSE 之前缺失原因**：v0.0.1-0.1.0 全是闭门 prototype，作者一直没顾上 OSS 形式化；README 早期就写了「See LICENSE」占位，但实文件漏建。
- 2026-05-14 README v0.1.0 刷新 + 用户/二次开发分流（PR #181）：用户提：「针对使用用户和二次开发的用户，进行区别对待」。`README.md` + `README.zh-CN.md` 同改：① tagline `WezTerm tabs` → `WezTerm 或 iTerm2 tabs` 反映 v0.1.0 第二终端落地 ② 加 v0.1.0 banner 链 release notes + conventions 修订记录 ③ 顶部加「Two audiences」callout 明确 TOC：Part 1 直接使用 / Part 2 二次开发（两 Part 早就存在但分流没标） ④ Repo layout 加 `packages/term-iterm2/` row ⑤ 「Adding a new terminal adapter」section 重写为 7 步反映 v1.13 patterns：TerminalIdSchema enum 加 / `TermAdapter.name` literal / 两步发送 / **TaggedDetector** chain（明确禁止 `typeof paneId` 反推终端 — issue 378 根因 framing）/ start.ts wizard wiring / tests 参考 term-iterm2 layout ⑥ Documentation pointers DD spec 链接展开，列名最近重点（iTerm2 / IMWork+IMOrigin / PermissionRequest bridge / credentials persistence）。`docs/architecture.md` 历史警告 header 更新到 v0.1.0 state，加 4 条 v1.13 架构 delta（PaneId 扩 brand `number|string` / detector chain `[wezterm, iterm2]` / IMWork 每终端拆 `IM<TermType>` / Stop file payload 带 termId）。无代码改动，typecheck 8 项绿。**未做（独立 follow-up）**：architecture.md 整版重写，wechat-era 内容仍在；本 PR 只更新历史警告 header + 关键 delta。
- 2026-05-14 v0.1.0 tag + GitHub release：lockstep version bump 跨 9 个 package.json（root + apps/multi-cc-im + 7 workspace packages：bridge / cli-cc / im-lark / shared / storage-files / term-iterm2 / term-wezterm）+ `cli.ts` VERSION 常量。**版本号语义**：0.0.x 是 IM-bridge 主链路打磨期（lark + wezterm），0.1.0 标记 **v1.13 iTerm2 第二 TermAdapter 全链路实装 + 关键根因修复**，包含一系列功能 + 真账号 smoke 暴露的硬骨头 bug 修。0.1.0 since 0.0.3 (PR #158)：v1.13 P1-P7 实装 — PR #160 DD lock / #162 P1 PaneId 类型扩 / #163 P2 term-iterm2 包 + Python helper / #164 P3 startup-resolver infra / #166 P4+P5 setup wizard + orchestrator wiring / #167 P6 docs / 真账号 smoke 暴露多 fix — #168 wizard 3 UX fix / #169 gate on not-installed / #170 mdfind detection / #171 daemon.log 诊断日志 / #172 lark WS retry-loop 改写 / #174 wizard empirical Python-API connect smoke / #175 IMWork → 每终端 IMWezterm + IMIterm2（issue 378 wezterm 漏进 IM）/ #176 iterm2-helper stderr 静音 / #177 hook 入口诊断 trace / #178 gate-level + parse-fail trace / #179 captureProcessLstart 强制 LC_TIME=C（issue 377 iTerm cc reply 不回 IM 根因 — locale-dependent ps -o lstart= 输出导致 isDaemonAlive 字符串比较失败 silent skip）。`pnpm install --frozen-lockfile` 通过（version bump 不动 resolution graph），966/966 tests pass，bundle 0.1.0 verified（281.93 KB），bin smoke 输出 `0.1.0`。
- 2026-05-13 v1.13 P2 实施落地：新 `packages/term-iterm2/` 包 + iTerm2 detector 注册。`bin/iterm2-helper.py` ephemeral Python 脚本（DD §6 C1 模式）— stdin JSON RPC（`{action:listSessions|sendText|sendKeystroke,...}`）/ stdout JSON 响应（`{ok,result|error}`），每次 invocation 开一个 iTerm2 WebSocket + 跑 action + 退出。`src/python-bridge.ts` Node 侧 spawn + JSON RPC（按 [memory: feedback_execfile_input_footgun](.claude/projects/.../memory/feedback_execfile_input_footgun.md) 用 spawn + 显式 child.stdin.end()），含 8s 超时 SIGKILL 兜底 + 错误信息 surface。`src/path-resolver.ts` 探测 `python3` binary（mirror wezterm path-resolver；不验证 iterm2 PyPI 包 importable — 那是 P4 wizard 的事）。`src/adapter.ts` `createITerm2Adapter` 实现 `TermAdapter & TermListPanes`，6 cap 全 cover（name='iterm2' / start no-op / sendText / sendKeystroke 加 empty 防御 / listPanes 走 cleanTitle + cleanCwd / stop no-op）。`src/tab-title.ts` cc emoji prefix strip + `Claude Code(\s*\[…\])?` 默认 title 折叠（mirror wezterm tab-title）。`cli-cc/pane-id-detectors.ts` 加 `detectIterm2PaneId`（解析 `w<W>t<T>p<P>:UUID`，strip 不稳 prefix，UUID 严格 hex+dash 36-char regex），`DEFAULT_DETECTORS` 改 `[detectWezTermPaneId, detectIterm2PaneId]`（wezterm 先，文档注释「ties 给 wezterm 因为更稳的 numeric id」）。`apps/multi-cc-im/package.json` 加 `@multi-cc-im/term-iterm2` workspace dep，`pnpm install --frozen-lockfile` 通过（新 workspace pkg 不动 resolution）。新 5 个 test files（adapter / python-bridge / path-resolver / tab-title / detector update）共 45 个新 case，全用 stub shell script 模拟 python3 + helper.py，覆盖 listPanes / sendText / sendKeystroke / timeout SIGKILL / 错误传播 / 空 key 防御 等。933/933 tests pass，bundle 0.0.3 verified（257.87 KB）。**P2 未做：path-resolver 不自动 install iterm2 PyPI 包（P4 wizard 工作）/ 没有持久 connection（C2 deferred to v2）/ helperScript path 由 caller 解析（P3 工作）**。

---

## 关键设计假设（状态总表）

> ✓ = 已 DD 锁定 / 协议事实 / 实测确认；⚠️ = 待实测；? = 待用户/DD 决策。

| 维度 | 状态 | 详情 |
|---|---|---|
| 协议层（飞书 IM）| ✓ | npm depend `@larksuiteoapi/node-sdk@^1.63.1`（官方 Bytedance 维护，MIT，月下载 4M+）；WSClient 长连接 = no-public-IP；IM v1 messaging covers text + image + file + interactive cards。**已删**：v1.4 时代 wechat / iLink vendored OpenClaw（PR #76/78/82 多次踩 undici 升级 instability，DD #86 §11.2 决定整包删）。[DD #86: Lark IM adapter](superpowers/specs/2026-05-09-lark-im-adapter-dd.md) + [历史 DD: iLink 选型](superpowers/specs/2026-04-26-ilink-library-dd.md)（保留作 wechat 时代决策记录）|
| Adapter 接口（IM/Term/CLI 事件流）| ✓ | TS-first hybrid（callback inject + extends-based 编译时 capability + type guard narrow）；[DD: adapter 接口](superpowers/specs/2026-04-29-adapter-interface-dd.md) |
| Storage（持久化策略）| ✓ | **无 SQL DB**：cc transcript 只读 cc jsonl，按需 tail；自身仅 cursor / friendly_name / ACL / pending msg buffer 4 项落 toml + JSONL；[DD: 持久化策略](superpowers/specs/2026-04-29-storage-strategy-dd.md) |
| 出站 / 入站 / Idle 唤醒 / Session 标识 / jsonl schema / pane-id | ✓ | [DD: hook+wezterm 实测](superpowers/specs/2026-04-27-cc-hook-wezterm-probe.md) |
| ACL（owner-only） | ✓ | 协议层自带过滤 |
| 多机（仅一台） | ✓ | 协议层硬约束（getupdates cursor 全局共享） |
| 路由语法 | ✓ | **G' 组合**：`#<name>` tmux 4 级 fallback（id / `=exact` / exact / 短前缀 / glob，歧义报错列候选）+ 空格分多目标（`#a #b ...`）+ `#all` 广播 + last-explicit-mention 粘性默认（带 visible echo）+ **bare `/<cmd>` 控制命令**（v1.4 起：`/list /help /current /start [off] /stop`；替换老 `@multi-cc-im /<cmd>` 语法，**无 backwards compat**；用户输入 `#<tab> /<cmd>` 仍正常 forward 给 cc TUI）；session 死自动 unset current；[DD: 路由语法](superpowers/specs/2026-05-04-routing-syntax-dd.md) + [DD #73: AI 路由](superpowers/specs/2026-05-09-ai-routed-im-dispatch-dd.md) |
| AI 路由 plain IM msg | ✓ | **每条 plain msg → daemon 起 `claude --print` 子进程做 triage**：headless、`--model claude-sonnet-4-6`（2026-05-13 从 Haiku 4.5 升 Sonnet 4.6，理由：Haiku 在多 sentence + 多 topic 复杂消息上 fragile — 漏 strip routing cue / 漏 rewrite 3rd-person pronouns；trade 1-3s 额外 latency 换 instruction-following robustness）、`--disable-slash-commands`、`--setting-sources user`、`--permission-mode bypassPermissions`、30s timeout、跑完即退、不沾用户 cc TUI 进程；输出 JSON `{target, intent, reason}`，daemon 拿 target 路由 + intent 当 cc prompt + echo 给 IM 端可见；任何错误（cc 不在 PATH / 网络 / 解析失败）→ 静默降级为 `❌ 无法识别目标，请用 #<tab>`，不阻断 daemon。**不**用 Anthropic API SDK（用户已有 cc Pro/Max 订阅，再付 API key 是反人类）；**不**长进程化（cc TUI 不为 daemon stdin/stdout 设计，per-msg spawn 简单可靠）。[DD: AI 路由 IM dispatch](superpowers/specs/2026-05-09-ai-routed-im-dispatch-dd.md) |
| 价格表来源 | ✓ DD 完成 | vendor LiteLLM Claude 子集 + `scripts/sync-prices.sh` 周期同步 + `config.toml [pricing]` user override；[DD: 价格表来源](superpowers/specs/2026-04-30-pricing-table-dd.md)。analytics package v2 实施时按此设计落地（v1 未实施 — Follow-up 列表）|
| 语音 / 图片 / 文件 | v2 推后 | DD #86 §8.4 锁定 v1 lark MVP = text + interactive cards。语音 / 图片 / 文件由 lark IM v1 audio/image/file msg_type 在 v2 加；v2 实施时按 §11.3 用 `~/.multi-cc-im/inbox/<imType>/<sid>/` 落本地。v1.4 wechat 时代有的 iLink voice_text + AES-128-ECB 解密路径已随 wechat purge 删除。|
| Pane-keyed state file 架构（DD #61）| ✓ | **路由 key = wezterm paneId**，daemon 不再追踪 sessionId / 单独检测 cc 活性。文件命名 `<paneId>_<sid>.<event>`（cc-hook 写）+ `<paneId>.IMOrigin`（daemon 写）；hook 入口 `WEZTERM_PANE` env undefined 直接静默 exit；matcher 只用 tab title（4 级 fallback：=strict / exact / prefix / glob），不接受 `#$<sid-prefix>` / `#$<paneId>`；用户可见警告：tab title 用纯数字易混淆。原 SessionStart / SessionEnd hook + PaneAlive verification 全部撤销。[DD: pane-keyed state files](superpowers/specs/2026-05-08-pane-keyed-state-files-dd.md) |
| Credentials 持久化 | ✓ | **0600 JSON 文件**（`~/.multi-cc-im/credentials/<im>.json` — e.g. `lark.json` `{appId, appSecret, savedAt}`）；**不调 OS keychain**（`@napi-rs/keyring` WSL 默认开箱失败 + Windows DPAPI 同用户进程互通防护有限 + 一致跨 IM）；[DD: credentials 持久化策略](superpowers/specs/2026-05-03-keychain-library-dd.md) |
| Permission prompt 转发 IM 审批 | ✓ | **PreToolUse hook + file IPC**：hook 子进程写 `<paneId>_<sid>.PermissionRequest.<id>.json` → daemon forward IM → 用户回 (a) 死语法 `#<tabname> /1`（允许）/`/2`（拒绝） 或 (b) **自然语言**（"multi-cc-im 那个 rm 同意" / "deny the bash one"，v1.7 起；daemon 把当前所有 pending Request 喂同一个 AI router 子进程，AI 决 target+decision+reason 写进 `permissionResponse` 输出字段；非对称信任：allow 必须含 match-signal 否则降级 deny；deny 自由）→ daemon 写 `<paneId>_<sid>.PermissionResponse.<id>.json`（AI 路径用 AI verbatim reason，死语法 fall back 默认串 `"IM user replied /1\|/2"`）→ hook 读完写 stdout `{permissionDecision: "allow"/"deny"}`。10 秒 timeout 默认 allow。AI 路径独立 `[AI permission] target=X decision=Y reason="..."` audit log 行（与 `[AI router]` 区分）。**AskUserQuestion 特判**（v1.9 起 / DD §9 D5-D 修订 2026-05-12）：cc 内置 widget 走 PreToolUse 但**不**走常规 allow/deny 语义；用 cc 官方 documented `allow + updatedInput.answers` 注入通道（[agent-sdk/user-input docs](https://code.claude.com/docs/en/agent-sdk/user-input#handle-clarifying-questions)）。setup-hooks.ts 把 PreToolUse 拆 2 disjoint matchers（AskUserQuestion 走 120s timeout / 其余走 20s）；hook decision tree 跳过 auto-allow short-circuit（IMWork on 任意模式都 forward）；orchestrator 渲染数字编号选项 + 「你的考虑」自由文本项替代 `/1` `/2`；router 检测 AUQ pending 分流到 AUQ 专用 AI prompt（输出 `{target, reason, answers: [{questionIndex, kind:'option'|'text', optionIndex|text}]}` 结构化 schema），daemon 查 `options[index-1].label` 组装 `answers` map；hook 写 `{permissionDecision: 'allow', updatedInput: {questions, answers}}` 让 cc 把 tool 当 succeeded 跑完，transcript 记录正常 tool result（不是 denied），**无 keystroke 注入**；timeout 110s 后 hook 自构造 `answers[q.question] = ''` 空字符走 allow + updatedInput（不走 deny 通道）；迟到 reply（hook 已退）daemon 给 IM 发「⏱ cc 已超时，本轮不再等待」提示。独立 `[AskUserQuestion forward pane=X tab=Y] questions=N options=M` audit log 行（与 `[AI router]` / `[AI permission]` 区分）。[DD: permission forward](superpowers/specs/2026-05-07-permission-forward-dd.md) + [DD: natural-language permission replies](superpowers/specs/2026-05-11-im-permission-natural-language-dd.md) + [DD: AskUserQuestion IM bridge](superpowers/specs/2026-05-12-askuserquestion-im-bridge-dd.md) |
| IM 模式总开关 (IMWork) + global IMOrigin + read-only 工具白名单 | ✓ | **三层组合**：`state/IMWork` JSON `{auto:bool}`（用户 bare `/start [off] /stop` 显式控制；**v1.7 起 bare `/start` 默认 `auto:true`**；`/start off` 切到 `auto:false` ask 模式；daemon start 自动重置为 OFF；老 0-byte 文件兼容视为 `{auto:false}`）+ **`state/IMOrigin` 单文件**（每条 inbound 覆盖 latest replyCtx；daemon start/stop 都删，跟 IMWork 同 always-fresh lifecycle，防 crash 残留 stale `context_token`；不再 per-pane / 不再 cc Stop 后 one-shot 删，[DD: IMOrigin global](superpowers/specs/2026-05-08-imorigin-global-dd.md) 修 stale token bug）+ Read/Grep/Glob/NotebookRead 自动 allow 不打扰 IM。hook PreToolUse 5 步：read-only → allow / IMWork null → **silent exit (defer 给 cc 原生 permission flow，user allow rules 优先命中)** / IMWork.auto=true → allow（trust mode，跳过 IM 转发）/ 无 IMOrigin → silent exit / 无 daemon → silent exit。**禁用 `permissionDecision: "ask"`** —— 它会 override user 已设的 "Yes don't ask again" allow rules。daemon reaper 10s 兜底删孤儿 PermissionRequest/Response。[DD: IMWork+IMOrigin](superpowers/specs/2026-05-08-imwork-imorigin-dd.md) + [DD: PreToolUse auto-approve](superpowers/specs/2026-05-08-pretooluse-auto-approve-dd.md) + [DD: IMOrigin global](superpowers/specs/2026-05-08-imorigin-global-dd.md) |
| Daemon liveness 检测 (PID lock + lstart 配对验证) | ✓ | `state/daemon.pid` JSON `{ pid, startedAt }`：daemon start 写 + 双开检测（已有 PID 活 + lstart 一致 → exit 1；stale lock → 覆盖）；daemon stop 删 IMWork + daemon.pid（Ctrl+C 清理）；hook PreToolUse + Stop 加 `isDaemonAlive()` short-circuit（`process.kill(pid, 0)` + `ps -o lstart=` 配对，防 OS PID 复用）。完整 hook decision tree 顺序：WEZTERM_PANE 入口 filter → read-only → IMWork → `<paneId>.IMOrigin` → daemon alive。state-sweep 用 wezterm `cli list` 的 paneId 集合做 ground truth，清掉 paneId 不在 live 集里的 orphan 文件 + stale daemon.pid（safe to run while daemon live：保留有效 lock）。[DD: daemon liveness](superpowers/specs/2026-05-09-daemon-liveness-dd.md) + [DD: pane-keyed state files](superpowers/specs/2026-05-08-pane-keyed-state-files-dd.md) |
| 监控 dashboard | ✓ | **local-only SSR web dashboard**：daemon start 时自启 hono + `@hono/node-server` web server，绑 `127.0.0.1:40719`（D1 固定），URL 打到 stderr 让用户点。视图组成 = 实时 daemon state（pid / uptime / active terminal / im adapter / im connection）+ pane 清单（live `termAdapter.listPanes()` 重新拉，含 `hasRenamed` 红字标记）+ 错误环形 buffer（`ErrorRingBuffer` 进程内 FIFO，N=200，orchestrator `onError` push 进来）+ 最近 cc session 成本（tail `~/.claude/projects/<slug>/<sid>.jsonl` 聚合 `message.usage`，价格 vendored LiteLLM Claude 4.x 子集，token×美元 linear sum）。**B0 纯内存**：无 SQL、无 JSONL 写盘、daemon 重启 buffer 清零；持久数据全在 cc transcript（外部 source of truth）。**C1 SSR + `<meta refresh content="5">`**：无 client JS、无 build step、5 秒整页刷新；附 `/api/{state,sessions,errors,cost}` JSON 路由给脚本/curl 拉。**包**：`@multi-cc-im/monitor`（`packages/monitor/`），JSX 仅在 `*.tsx` 内部，entry `index.ts` 走 `renderDashboard()` 字符串边界，consumer tsconfig 不要 `--jsx`。**端口冲突容错**：startMonitor 抛 error 时 daemon 仍跑（IM bridge 不阻塞，stderr 提示 "port 40719 in use? bridge still runs without dashboard"）。[DD: cc monitor dashboard](superpowers/specs/2026-05-15-cc-monitor-dashboard-dd.md) |
| Interactive start/setup wizard | ✓ DD 完成 | **单 `multi-cc-im start [<adapter>]` 命令**：无参 → `@clack/prompts` arrow-key 菜单选 IM adapter（光标默认聚焦在已配置过的 adapter，由 `~/.multi-cc-im/credentials/<adapter>.json` 文件存在性判定）；有参 → 直接选指定 adapter；选定后查凭据 — 已配置则进 daemon 主循环，未配置则显示 `❌ <adapter> 未配置 \| [开始配置] [返回]` —— 选 [开始配置] 进 schema-driven setup wizard，wizard 跑完同进程续 daemon；选 [返回] 回菜单（无参情况）/ 退出 1（有参情况）。`!process.stdin.isTTY` + 无参数 → exit 1 + hint。**Setup wizard 接口**：每个 IM adapter 包导出 `setupSchema: AdapterSetupSchema`（zod schema 含 per-field metadata `{ key, label, hint, secret }`）+ `validate(values): Promise<void>` callback；CLI 通用 wizard 按 schema 渲染 prompts，secret 字段用 AWS CLI 风格 mask `'*'*16 + last_4`（非 secret 字段全显）；编辑已存凭据时显示当前默认值，回车保留 / 输入新值替换。**配置指南**：`docs/setup-feishu.md`（每个 IM 一份）作为 source of truth，wizard 启动时 `terminal-link` v5 渲染含 OSC 8 hyperlinks 的 inline ASCII 步骤，不支持 OSC 8 的终端降级 plain-text；可选 `open` v11 跳浏览器（容错 SSH/headless）。**Cancel 语义**：`@clack/prompts` 返回 sentinel `cancel symbol` + `isCancel(value)` 检查，无 throw，daemon 不被 SIGINT 杀。**保留 `multi-cc-im login <adapter> --app-id ... --app-secret ...` 非交互 shortcut** 给 CI / 脚本自动化场景，走同一 schema 写盘。[DD: interactive start wizard](superpowers/specs/2026-05-10-interactive-start-wizard-dd.md) |

---

## 项目特定技术规范

CLAUDE.md 包含跨项目通用的工程纪律（TS strict / no `any` / no 裸 catch / 凭据 0600 / TDD 节奏 / etc.）。本节是 multi-cc-im **特定** 实现规范 —— 写跟 cc hook / wezterm cli / IM adapter / IPC 文件相关的代码时**必须**遵守。

| 规范 | 备注 |
|---|---|
| **Hook 内部 timeout < cc-side hook timeout** | cc settings.json 给 hook 的 `timeout` 是 cc kill 子进程的 deadline；hook 自己 `PERMISSION_TIMEOUT_MS` 必须**小于**它（PR-G：10s vs 20s）。10s margin 覆盖：(1) hook 写 stdout + cleanup；(2) daemon-side IM send 重试 / 抖动预算；(3) 网络抖动。否则 race 时 cc 拿不到 hook decision、行为不确定 |
| **send-text 注入两步法** | Step1 默认 paste 内容（任意 `\n` / 元字符 / Unicode 安全），Step2 `--no-paste $'\r'` 提交。混用 `--no-paste` 发内容 = 注入面（cc TUI 解释快捷键）|
| **multi-cc-im hook 不许写非协议 stdout** | cc 把 hook stdout 当 system context 注入（attachment 机制）→ 烧 token + 行为不可预测。受控 JSON（`{"decision":"block",...}` / PreToolUse `{hookSpecificOutput:...}`）除外，其他一律走 stderr 或文件 |
| **idle 唤醒用 `stop_hook_active` 防死循环** | Stop hook 处理时先 `if (stdin.stop_hook_active) return;`。stdin 字段是 cc 原生防护，零 race，比文件标记可靠 |
| **路由解析 = `WEZTERM_PANE` env** | hook env 直接给，O(1)。禁用 `wezterm cli list` 解析 cwd 反推 pane-id（O(N) + 多 cc 同 cwd 时歧义） |
| **`#<name>` 只匹配 tab title** | 不接受 `#$<sid-prefix>` / `#$<paneId>` — daemon 不再持 sessionId，paneId 是数字易跟 tab title 撞。用户 tab title 用纯数字 → `/start` echo 主动警告。session 死 = 用户的 cc TUI 死 = 用户自己重开新 tab；daemon 不做 pane-alive 验证，每次 IM 事件直接调 `wezterm cli list`（[DD: pane-keyed state files](superpowers/specs/2026-05-08-pane-keyed-state-files-dd.md)）|
| **路由 visible echo 必须有** | bot 把消息派给 cc 前后必须给 IM 端可见反馈（`→ frontend received` / `📌 current = frontend` / `⚠️ frontend disconnected, current cleared`），否则 last-explicit-mention 粘性的 current_session 状态跟用户脑模型会错配（[DD: 路由语法](superpowers/specs/2026-05-04-routing-syntax-dd.md) 第 4 步理由 3） |
| **路由 key 用 `CLAUDE_PROJECT_DIR` 或 `stdin.cwd`** | 已 realpath；不要用 `PWD` env（macOS `/tmp` vs `/private/tmp` 不一致） |
| **不修改 cc 自己的 jsonl** | `~/.claude/projects/**/*.jsonl` 只读；任何写入都是 bug |
| **凭据落本地路径约定** | `~/.multi-cc-im/credentials/<im>.json` (mode 0600) — 通用规则在 CLAUDE.md「凭据 0600 落盘」；具体 schema (`{appId, appSecret, savedAt}` 等) 见 [`packages/im-lark/src/credentials.ts`](../packages/im-lark/src/credentials.ts) |

---

## 项目特定禁止清单

通用禁令（TS `any` / shell 字符串拼接 / 动态代码求值 / 凭据明文 / 同步阻塞 hook / 跳过 DD / etc.）见 [`CLAUDE.md`](../CLAUDE.md)「禁止清单」。下列是 multi-cc-im **特定** 禁令（绑 cc / wezterm / IM 协议 / IPC 文件）：

- 托管 / spawn cc 进程（用户的 cc TUI 实例）
- 修改 cc 的 jsonl（`~/.claude/projects/**/*.jsonl`）
- 用非官方 / 灰产 / 逆向协议（仅官方 IM SDK；当前 = `@larksuiteoapi/node-sdk`）
- 公网传输用户 prompt（含外部图床）
- 不带 cursor 的长轮询（如果适用 — lark WSClient 不需要，未来 IM 可能需要）
- "[执行命令]" 注入字段直接落 cc
- adapter 间直接 import（违反 4 维度可扩展）
- 把 share 对话当 ground truth
- hook 写非协议 stdout（污染 cc context）
- 用 `PWD` 做路由 key（须用 `CLAUDE_PROJECT_DIR`）
- 用 `wezterm cli list` 解析 cwd 反推 pane-id（须用 `WEZTERM_PANE` env）
- send-text 单步带回车（须分两步）
- hardcode 外部 CLI 绝对路径（wezterm 等须运行时探测）
- 自动 last-reply-to 粘性（cc 最后回复的 session 自动当下次 current_session — Cline #3514 用户反弹证伪）
- 路由 `#<前缀>` 歧义时自动挑第一个（须报错列候选 — tmux/screen/Vim/aider 全行业拒绝）
- `#$<sid-prefix>` / `#$<paneId>` 路由（DD #61：matcher 只用 tab title，纯数字 tab 由 `/start` 警告）

---

## 参考资料

### DD 报告

- Lark/Feishu IM adapter（**当前**，DD #86）: [`superpowers/specs/2026-05-09-lark-im-adapter-dd.md`](superpowers/specs/2026-05-09-lark-im-adapter-dd.md)
- cc Hook + wezterm cli 行为实测（**已完成**）: [`superpowers/specs/2026-04-27-cc-hook-wezterm-probe.md`](superpowers/specs/2026-04-27-cc-hook-wezterm-probe.md)
- iLink 协议库选型（**v1.4 wechat 时代历史**，DD #86 §11.2 已 supersede）: [`superpowers/specs/2026-04-26-ilink-library-dd.md`](superpowers/specs/2026-04-26-ilink-library-dd.md)
- 全部 DD 报告：[`superpowers/specs/`](superpowers/specs/)

### 工程文档

- [`architecture.md`](architecture.md) — 架构图 / 包依赖 / 目录结构 / 数据存储（toml + 0600 凭据 + state files）/ 外部 CLI 路径策略
- [`competitors.md`](competitors.md) — 不直接采用的端到端项目
- [`dev.md`](dev.md) — 开发命令 / TDD 节奏 / 调试技巧
- [`../CLAUDE.md`](../CLAUDE.md) — AI 干活纪律（找根因 / DD 流程 / 编码行为准则）

### 上游文档

- Lark/Feishu Node SDK: https://github.com/larksuite/node-sdk
- Feishu Open Platform IM v1: https://open.feishu.cn/document/server-docs/im-v1/message/create
- Feishu event subscription (WSClient long-connection): https://open.feishu.cn/document/server-docs/event-subscription-guide/event-subscription-configure-/request-url-configuration-case
- Claude Code Hook: https://code.claude.com/docs/en/hooks
- Claude Code Permissions: https://code.claude.com/docs/en/permissions
- WezTerm CLI: https://wezterm.org/cli/cli/index.html | send-text: https://wezterm.org/cli/cli/send-text.html

### 输入材料归档（gitignored，**不作 ground truth**）

- 设计对话原文：`.playwright-mcp/share-fulltext-*.json`
- 端到端项目调研：`.playwright-mcp/research-deep.txt`
